
module(
    name = "aws_c_event_stream_test",
    version = "0.0.0",
    compatibility_level = 1,
    bazel_compatibility = [">=7.2.1"],
)

bazel_dep(name = "aws_c_event_stream", version = "0.5.4")

bazel_dep(name = "bazel_skylib", version = "1.7.1")
