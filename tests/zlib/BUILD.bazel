load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
  name = "zlib_test",
  targets = ["@zlib//:zlib"],
)

build_test(
  name = "zlib_opt_includes_test",
  targets = ["@zlib//:zlib_opt_includes"],
)
