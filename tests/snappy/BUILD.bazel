load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
  name = "snappy_test",
  targets = ["@snappy//:snappy"],
)

build_test(
  name = "snappy-c_test",
  targets = ["@snappy//:snappy-c"],
)
